import 'package:flutter/material.dart';

class ExchangeRateScreen extends StatelessWidget {
  const ExchangeRateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Exchange Rate Screen');
  }
}
